
ResultSetExtractor
RowCallbackHandler
(batch operations)
alternate version using indexed params instead of named?

might be interesting to load mappers by class as alternate

@RowMapper(PersonRowMapper.class)

which would do a type look up rather than name